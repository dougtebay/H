<nav class="navbar navbar-default navbar-fixed-top" id='navbar-partial'>
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if @user %>
        <%= link_to image_tag('logo.png', class: 'logo'), user_path(@user) %>
      <% else %>
        <%= link_to image_tag('logo.png', class: 'logo'), root_path %>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <% if @user %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Prescriptions<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'My Prescriptions', prescriptions_path, method: 'get' %></li>
            <li><a id='newPrescriptionButton' href="#" data-toggle="modal" data-target="#prescriptionModal">New Prescription</a></li>
          </ul>
        </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
              <% if @user %>
              <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="   false"><%= "#{@user.first_name} #{@user.last_name}" %><span class="caret"></span></a>
      <ul class="dropdown-menu">
            <div id='<%= @user.id %>' class='user-id' hidden></div>
             <li><a id='editProfileButton' href="#" data-toggle="modal" data-target="#userModal">Edit Profile</a></li>
             <li><%= link_to 'Log Out', logout_path, method: 'get' %></li>
      </ul>
              </li>
            <% else %>
              <button type="button" class="btn btn-default navbar-btn" data-toggle="modal" data-target="#signUpModal">
                Sign Up
              </button>
              <button type="button" class="btn btn-default navbar-btn" data-toggle="modal" data-target="#loginModal">
              Log In
              </button>

            <% end %>
          </div>
        </nav>
      </ul>
    </div>
  </div>
</nav>