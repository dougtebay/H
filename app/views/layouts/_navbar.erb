<nav class="navbar navbar-default navbar-fixed-top" id='navbar-partial'>
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_user %>
        <%= link_to image_tag('logo.png', id: 'harmony-logo'), user_path(current_user), remote: true %>
      <% else %>
        <%= link_to image_tag('logo.png', id: 'harmony-logo'), root_path %>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <% if current_user %>
        <li class="dropdown">
          <a href="#" id='prescriptions-dropdown' class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Prescriptions<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'My Prescriptions', prescriptions_path, method: 'get', id: 'my-prescriptions-button', remote: true %></li>
            <li><a id='new-prescription-button' href="#" data-toggle="modal" data-target="#prescription-modal">New Prescription</a></li>
          </ul>
        </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
              <% if current_user %>
              <li class="dropdown">
              <a href="#" id='user-dropdown' class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="   false"><%= "#{current_user.first_name} #{current_user.last_name}" %><span class="caret"></span></a>
      <ul class="dropdown-menu">
            <div id='<%= current_user.id %>' class='user-id' hidden></div>
             <li><a id='edit-user-button' href="#" data-toggle="modal" data-target="#edit-profile-modal">Edit Profile</a></li>
             <li><%= link_to 'Log Out', logout_path, method: 'get', remote: true, id: 'log-out-button' %></li>
      </ul>
              </li>
            <% else %>
              <button type="button" class="btn btn-default navbar-btn" data-toggle="modal" data-target="#sign-up-modal" id='new-user-button'>
                Sign Up
              </button>
              <button type="button" class="btn btn-default navbar-btn" data-toggle="modal" data-target="#login-modal">
              Log In
              </button>

            <% end %>
          </div>
        </nav>
      </ul>
    </div>
  </div>
</nav>