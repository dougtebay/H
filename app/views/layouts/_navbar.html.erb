<nav id='navbar-partial' class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_user %>
        <a href="#"><%= image_tag 'logo.png', id: 'harmony-logo' %></a>
      <% else %>
        <%= image_tag 'logo.png', id: 'harmony-logo' %>
      <% end %>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if current_user %>
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Prescriptions<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="#" id='my-prescriptions-button'>My Prescriptions</a></li>
            <li><a href="#" id='new-prescription-button' data-toggle="modal" data-target="#prescription-modal">New Prescription</a></li>
          </ul>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#" id='<%= current_user.id %>' class='edit-user-button' data-toggle="modal" data-target="#edit-profile-modal"><%= "#{current_user.first_name} #{current_user.last_name}" %></a></li>
        <li><a href="#" id='log-out-button'>Log Out</a></li>
        <% else %>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#" id='new-user-button' data-toggle="modal" data-target="#sign-up-modal">Sign Up</a></li>
        <li><a href="#" data-toggle="modal" data-target="#login-modal">Log In</a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>